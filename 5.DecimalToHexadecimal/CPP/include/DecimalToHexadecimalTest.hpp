#ifndef DECIMALTOHEXADECIMAL_TEST_HPP
#define DECIMALTOHEXADECIMAL_TEST_HPP

#include <string> // Include this line for std::string
#include <vector>

class DecimalToHexadecimalTester {
public:
    bool test(int decimal, const std::string& expectedHexadecimal);
};

#endif
